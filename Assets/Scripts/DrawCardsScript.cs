using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DrawCardsScript : MonoBehaviour
{

    public GameObject card1, card2, playerArea, secondPlayerArea;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void onClick(){
        //GameObject playerCard = Instanciate(card1, new Vector3);
    }
}
